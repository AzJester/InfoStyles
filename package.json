{
  "name": "infostyles",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Infographic & slide style library with NotebookLM / OpenAI image prompts and an admin-only AI style creator.",
  "scripts": {
    "build": "python3 build_styles.py",
    "test": "node --test"
  },
  "dependencies": {
    "@vercel/blob": "^0.27.0",
    "@vercel/kv": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
